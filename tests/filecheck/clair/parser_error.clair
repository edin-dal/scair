// RUN: clair-run %s 2>&1 | filecheck %s

{ type tt = dialect.name1 }
{ type gg = dialect.name2 }

-> type
----------------- NameAttr
-> dialect.name1

-> operands   [map:gg, map==gg, map3 : tt | gg]
----------------- NameOp
-> dialect.name2

// CHECK:       The clair parser is on holidays right now. Please try again later.
