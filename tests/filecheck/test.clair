// RUN: clair-run %s | filecheck %s

{ type tt = dialect.name1 }
{ type gg = dialect.name2 }

-> type
----------------- NameAttr
-> dialect.name1

-> operands   [map:gg, map2==gg, map3 : tt | gg]
----------------- NameOp
-> dialect.name2

// CHECK: HashMap(dialect -> Dialect(dialect,ListBuffer(Operation(dialect.name2,NameOp,List(Operand(map,Any(List(RegularType(dialect,name2)))), Operand(map2,Equal(RegularType(dialect,name2))), Operand(map3,Any(List(RegularType(dialect,name2), RegularType(dialect,name1))))),List(),Region(0),Successor(0),List(),List())),ListBuffer(Attribute(dialect.name1,NameAttr,List(),1))))