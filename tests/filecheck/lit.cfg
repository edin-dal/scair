import lit.formats
import os

config.name = "ScaIR"
config.test_format = lit.formats.ShTest()
config.suffixes = ['.mlir']

scair_opt = "../../target/universal/stage/bin/scair-opt"
config.substitutions.append(('scair-opt', f"{scair_opt}"))
